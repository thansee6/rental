{
    "name": "rental-property-platform",
    "version": "1.0.0",
    "description": "LuxeSpace Rental Property Platform",
    "scripts": {
          "install-backend": "npm install --prefix backend",
          "install-frontend": "npm install --prefix frontend --include=dev || npm install --prefix frontend --production=false",
          "build-frontend": "npm run build --prefix frontend",
          "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
          "start": "node backend/server.js"
    }
}
